[project]
name = "nf26-smart-teem"
version = "0.1.0"
description = "SID installation tooling for Snowflake"
requires-python = ">=3.10"
dependencies = [
    "apache-airflow",
    "dbt-snowflake>=1.11.5",
    "snowflake-connector-python",
]

